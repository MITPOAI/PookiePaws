# Optional remote generation providers. The MVP works with --provider mock without keys.
RUNWARE_API_KEY=
FAL_KEY=

# Optional runtime home. Defaults to ~/.pookiepaws.
POOKIEPAWS_HOME=

# Optional Python executable for the FFmpeg render helper.
PYTHON=

# Optional FFmpeg executable when ffmpeg is not on PATH.
FFMPEG=
